[tools]
bun = "1.2.17"

[tasks.dev]
run = "bun src/cli.ts"

[tasks.install]
run = "bun install"

[tasks.test]
run = "mise run install && bunx vitest run"

[tasks.ci]
run = "mise run test"

[tasks.build]
run = "mise run install && bun scripts/build-release.ts"
